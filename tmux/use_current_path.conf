# Contains tmux key bindings for tmux >= 1.9 to 
# bind new window/pane operations to use the current
# working directory in the new pane

bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"
